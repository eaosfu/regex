matches
alternated_matches
matches alternated_matches
alternated_matches matches
matches alternated_matches matches

matches: (up to 256 chras long)
  one char literal
  BOL_anchored one char literal
  EOL_anchored one char literal

  multi_char literal
  BOL_anchored multi_char literal
  EOL_anchored multi_char literal

  quantified one char literal
  BOL_anchored quantified one char literal
  EOL_anchored quantified one char literal

  quantified multi_char literal
  BOL_anchored quantified multi_char literal
  EOL_anchored quantified multi_char literal

  one char range
  BOL_anchored one char range
  EOL_anchored one char range

  multi_char range
  BOL_anchored multi_char range
  EOL_anchored multi_char range

  quantified one char range
  BOL_anchored quantified one char range
  EOL_anchored quantified one char range

  quantified multi_char range
  BOL_anchored quantified multi_char range
  EOL_anchored quantified multi_char range

alternated matches:
  matches|matches
  matches|matches|matches
  matches|matches|matches|matches...|matches {256 characters long}
